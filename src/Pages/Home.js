import Navbar from './navbar'
import '../CSS/Home.css'

const Home = (props) => {
    return (
        <div>
            <p className="Heading1">CourseMan!</p>
            <Navbar />
        </div>
        );
};

export default Home;